// Payment Service — Kafka Consumer + Producer
// Consumes from "orders" topic, processes payment, publishes to "payment-success".

const SERVICE_NAME = "payment-service";

console.log(`[${SERVICE_NAME}] Starting...`);
console.log(`[${SERVICE_NAME}] TODO: Implement Kafka consumer (orders) + producer (payment-success)`);
