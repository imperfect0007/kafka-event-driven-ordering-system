// Inventory Service — Kafka Consumer + Producer
// Consumes from "payment-success", updates inventory, publishes to "inventory-updated".

const SERVICE_NAME = "inventory-service";

console.log(`[${SERVICE_NAME}] Starting...`);
console.log(`[${SERVICE_NAME}] TODO: Implement Kafka consumer (payment-success) + producer (inventory-updated)`);
