// Order Service — Kafka Producer
// Accepts incoming orders and publishes events to the "orders" topic.

const SERVICE_NAME = "order-service";

console.log(`[${SERVICE_NAME}] Starting...`);
console.log(`[${SERVICE_NAME}] TODO: Implement REST API + Kafka producer`);
